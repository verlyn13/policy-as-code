apiVersion: infisical.io/v1
kind: MachineIdentity
metadata:
  name: token-service
  namespace: infisical-journal
spec:
  type: runtime
  environment: prod
  project: journal
  description: "Machine identity for token-service"
